before_script:
  - apt-get update -qq && apt-get install -y -qq python2 python2-dev python3 python3-dev pypy
  - pypy -m pip install -r requirements.txt
  - python2 -m pip install tox
  - python3 -m pip install tox

flask:
  script:
    - pypy app4flask/app.py
    - tox